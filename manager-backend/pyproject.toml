[project]
name = "manager-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Peter-Vegvari", email = "vegvaripeter21@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "netconf-console2",
    "netconf-client",
    "basedpyright",
    "ruff",
    "pytest",
    "pytest-asyncio",
    "pytest-cov",
    "fastapi[standard]",
    "pydantic-settings",
    "uvicorn",
    "lxml",
    "types-lxml",
    "yangson",
    "wireup>=2.9.0",
]

[project.scripts]
manager-backend = "app:main"

[project.urls]
Homepage = "https://github.com/Peter-Vegvari/NETCONF-manager"

[build-system]
requires = ["uv_build>=0.10.10,<0.11.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "app"
module-root = ""

[tool.basedpyright]
venvPath = "."
venv = ".venv"
include = ["app", "tests"]
defineConstant = { DEBUG = true }
reportImportCycles = false
reportAny = false
reportExplicitAny = false
reportUnusedCallResult = false
reportUnusedParameter = false
allowedUntypedLibraries = ["ncclient", "app", "yangson"]

[tool.pytest.ini_options]
minversion = "8.4.1"
addopts = "-ra -q"
testpaths = ["tests"]

[tool.ruff.lint]
extend-select = ["I"]
fixable = ["I"]

[tool.ruff.lint.isort]
known-first-party = ["app"]
