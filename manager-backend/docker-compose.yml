services:
  manager-backend:
    build: .
    container_name: manager-backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - /app/.venv
      - ./resources:/resources
      - shared:/shared
    networks:
      - shared
    
  # notconf:
  #   image: ghcr.io/notconf/notconf-ietf:latest
  #   container_name: notconf
  #   ports:
  #     - "830:830"
  #     - "80:80"
  #   networks:
  #     - shared

networks:
  shared:
    name: shared

volumes:
  shared:
    name: shared