services:
  manager-backend:
    build: .
    container_name: manager-backend
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
      - ./resources/yang-modules:/resources/yang-modules
    
  notconf:
    image: ghcr.io/notconf/notconf-ietf:latest
    container_name: notconf
    ports:
      - "830:830"
      - "80:80"