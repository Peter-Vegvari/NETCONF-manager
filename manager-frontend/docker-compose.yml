services:
  manager-frontend:
    build: .
    container_name: manager-frontend
    user: "${UID:-0}:${GID:-0}"
    ports:
      - "3000:5173"
    volumes:
      - ./src:/app/src
    networks:
      - shared

networks:
  shared:
    name: shared
