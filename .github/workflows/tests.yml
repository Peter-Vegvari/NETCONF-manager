# .github/workflows/tests.yml
name: Integration Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: manager-backend

    steps:
      - uses: actions/checkout@v4

      - run: docker compose -f docker-compose.yml -f docker-compose.test.yml -p test up -d --build
      
      - run: docker compose -f docker-compose.yml -f docker-compose.test.yml -p test exec manager-backend pytest
      
      - run: docker compose -f docker-compose.yml -f docker-compose.test.yml -p test down